// PlaybackOutputIndicator removed - MPD status is now indicated by the
// speaker icon color in PlaybackOutputSelector (primary when MPD selected)
// Connection status details are available in the tooltip on hover.

export const PlaybackOutputIndicator = () => {
    // Return null - indicator functionality merged into PlaybackOutputSelector
    return null;
};
